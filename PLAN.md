# Plan de migration multiplateforme — transcriber

> Ce fichier est la seule source de vérité de l'avancement. Il doit refléter la réalité à tout moment. Commit à chaque mise à jour.
> Dernière mise à jour : 2026-08-14 — statut : ✅ MVP portable + release automatisée + backlog packaging livrés

Ce plan découle directement de [`AUDIT.md`](./AUDIT.md). Résumé du raisonnement :

- L'audit (§1) montre que le code métier et le code Tkinter/OS sont entièrement entremêlés dans un seul fichier avec état global. **Aucun portage propre n'est possible sans séparer d'abord `domain/` (logique pure) et `platform/` (adaptateurs OS/GUI)** — c'est donc le tout premier jalon, avant même de toucher à Windows/Linux.
- L'audit (§4, §6) montre qu'il n'existe **aucun test ni CI**. Refactorer sans filet serait risqué et invérifiable sur les OS que je ne peux pas exécuter localement (§4 du brief) — donc la mise en place de `pytest` est intégrée au même jalon que l'extraction du domaine, et la **CI multi-OS est mise en place dès le jalon suivant**, avant tout code spécifique Windows/Linux, conformément à la contrainte du brief.
- L'audit (§5) tranche qu'**aucun fallback GPU n'est à coder** : `ctranslate2` gère déjà `device="auto"` (CPU partout, CUDA en bonus) de façon identique sur les trois OS. Aucun jalon dédié à l'accélération matérielle n'est donc nécessaire.
- L'audit (§5) tranche le format de distribution : **PyInstaller pour les trois OS**, en miroir de la pratique macOS actuelle (bundle `--onedir` zippé, pas d'installeur). Les jalons Windows/Linux réutilisent donc l'outillage existant plutôt que d'en introduire un nouveau.
- L'audit (§4) identifie un blocage Linux spécifique et non négociable : `python3-tk` est une dépendance **système**, absente de `pip`. Le jalon Linux doit la documenter et l'installer explicitement en CI.
- Deux corrections de qualité indépendantes du portage mais peu coûteuses et à faible risque sont glissées dans le jalon 1 (bug latent §6, nettoyage `requirements.txt` §7-3) car elles touchent exactement le code qui est de toute façon réécrit à cette étape.
- Le jalon MVP portable correspond à la définition imposée par le brief (§3) : lancement + transcription bout-en-bout vérifiés par CI verte sur macOS Intel, macOS Apple Silicon, Windows et Linux.

Contrainte transverse à tous les jalons : **aucune régression macOS** (vérifiée par la CI dès qu'elle existe, par lecture de code avant).

### Écarts documentés par rapport au brief initial

- Le paquet `platform/` prescrit par le brief a été nommé **`platform_/`** (avec un underscore) pour ne pas masquer le module standard Python `platform`, qui aurait cassé des imports internes de bibliothèques tierces. Détail en `J1-6` ci-dessous.
- La CI ne s'appuie **plus sur un runner `macos-13`** : ce runner n'a jamais pu être attribué par GitHub sur 4 tentatives et plus d'1h30 d'attente, pour une cause non diagnosticable depuis l'API publique. **Résolu autrement** : le Python fourni par `actions/setup-python` sur `macos-latest` (Apple Silicon) est un binaire universal2 (x86_64 + arm64), et Rosetta 2 fonctionne sans problème sur ce runner — la CI construit donc un vrai environnement x86_64 via `arch -x86_64` et y exécute toute la suite (y compris le test E2E de transcription réelle), ce qui referme la couverture Intel sans dépendre de `macos-13`. Détail complet en `AUDIT.md` « Annexes — résolution : Intel via Rosetta sur macos-latest ».

---

## Jalons

- **J0** — Audit et plan — Produire `AUDIT.md` puis en déduire ce plan avant tout code.
- **J1** — Extraction domaine + qualité — Séparer `domain/`/`platform_/`, poser `pytest`, corriger le bug latent, nettoyer `requirements.txt`.
- **J2** — CI multi-OS — Matrice GitHub Actions (macOS/Windows/Linux) posée avant tout code spécifique à un OS cible.
- **J3** — Portage Windows — Build PyInstaller Windows documenté et vérifié en CI.
- **J4** — Portage Linux — Build PyInstaller Linux + dépendance `python3-tk` documentée et vérifiée en CI.
- **J5** — MVP portable — E2E réel + CI verte simultanée sur les 4 configurations cibles, README à jour.
- **J6** — Release automatisée — `python-semantic-release` + attachement des binaires PyInstaller à chaque release GitHub.
- **J7** — Packaging avancé (backlog post-MVP) — AppImage Linux et installeur Windows, au-delà de l'équivalence fonctionnelle minimale du MVP.
- **J8** — Qualité additionnelle — Nettoyage identifié lors de la relecture finale de l'audit (icône dupliquée, branche morte, linter).
- **J9** — Corrections post-test réel — Deux bugs remontés par l'utilisateur en testant les binaires livrés sur Windows et Linux réels (blocage UI Windows, binaire Linux lié à une glibc trop récente) ; la CI verte sur les 4 configurations cibles ne les avait pas détectés (point aveugle documenté en `AUDIT.md` addenda).

---

## 🔵 En cours

- [ ] `J9-4` Diagnostic du blocage Windows persistant après `J9-1` : traces `[DEBUG]` temporaires ajoutées (`platform_/transcriber.py`, `app_whisper.py`) + build de test sans `--windowed` (console visible). Sortie console obtenue de l'utilisateur : le programme se bloque juste après le téléchargement de `config.json` (rapide, ~0s), avant tout progrès sur le fichier de poids du modèle — donc **pas** un problème de threading Tk (déjà exclu par le succès sous Linux avec le même code), mais un blocage réseau pendant le téléchargement du modèle sur Hugging Face Hub, cohérent avec un réseau restrictif (pare-feu/proxy d'entreprise autorisant `huggingface.co` mais bloquant/dropant silencieusement le CDN de fichiers volumineux). `huggingface_hub==0.30.2` a déjà un timeout par défaut de 10s et jusqu'à 5 tentatives (`http_backoff`, ~90s d'attente cumulée dans le pire cas) — resserré à 8s (`HF_HUB_DOWNLOAD_TIMEOUT`, fixé avant l'import de `faster_whisper`) pour borner l'attente sans l'affaiblir sur une connexion simplement lente. **Limite assumée** : si le réseau de l'utilisateur droppe silencieusement les paquets au niveau TCP plutôt que de refuser la connexion, aucun changement côté code applicatif ne peut garantir un échec propre — seul un test sur un autre réseau (hotspot personnel, etc.) peut confirmer l'hypothèse réseau. Traces `[DEBUG]` et build console à retirer une fois confirmé (avant fusion).
  > ⚠️ Bloqué (probable limite réseau externe, hors contrôle du code applicatif) : retesté par l'utilisateur en attendant plus longtemps — toujours aucun dialogue d'erreur, blocage identique malgré le timeout resserré à 8s. Renforce l'hypothèse d'un vrai « black hole » réseau (paquets silencieusement ignorés plutôt que connexion refusée), contre lequel `requests`/`huggingface_hub` ne peut rien garantir côté client. Suivi dans l'issue GitHub [#2](https://github.com/aymnms/transcriber/issues/2). Seul un test de l'utilisateur sur un autre réseau (hotspot personnel, etc.) peut confirmer/infirmer définitivement l'hypothèse ; aucune action de code supplémentaire n'est planifiée tant que ce n'est pas fait. Ne bloque pas `J9-1`/`J9-2` (confirmés fonctionnels indépendamment) mais reste un défaut UX réel : en cas de blocage réseau total, l'utilisateur n'a toujours aucun retour visuel avant un temps indéterminé.
- [ ] `J9-3` Build manuel de test (`.github/workflows/manual-build.yml`, temporaire, non lié à `release.yml`) déclenché pour fournir des binaires à tester avant fusion : le job Windows a réussi (artefact `Transcriber-Windows` prêt), mais l'étape « Build AppImage » échoue dans le conteneur `ubuntu:22.04` (`Process completed with exit code 1`, détail indisponible sans connexion GitHub). Le tarball Linux (`transcriber-Linux.tar.gz`, qui contient le correctif `J9-2`) est désormais uploadé indépendamment de l'AppImage pour ne pas bloquer le test utilisateur. **Bloquant pour la fusion sur `main`** : `release.yml` (`build-linux`) contient la même séquence AppImage et échouerait pareil en production — à corriger avant toute fusion, pas seulement un problème du workflow de test.
  > 🔵 En cours : diagnostic en cours, logs complets à obtenir (soit une fois signalé par l'utilisateur comme précédemment pour le bug glibc, soit par une nouvelle reproduction).

## ⬜ À faire

**Backlog (hors périmètre MVP, réf. `AUDIT.md` §7 point 7)**
- [ ] `BL-1` Codesign/notarization macOS — nécessite un compte Apple Developer Program actif (99$/an), non disponible actuellement ; discuté avec l'utilisateur le 2026-08-14, reporté
- [ ] `BL-2` Si un vrai Mac Intel ou un runner `macos-13` fonctionnel devient disponible un jour, envisager une vérification ponctuelle sur matériel physique en complément de la CI Rosetta (non bloquant : Rosetta est déjà une vérification x86_64 authentique, pas une simulation)
- [ ] `BL-3` Bumper `actions/checkout`/`actions/setup-python` vers une version nativement Node 24 quand elle sortira (actuellement des warnings inoffensifs — GitHub force déjà le fallback Node 24, la CI reste verte, cf. journal 2026-08-14)

## ✅ Terminé

- [x] `J0-1` Audit complet (`AUDIT.md`)
- [x] `J0-2` Rédaction de ce plan
- [x] `J1-1` Créer la branche de travail dédiée (`feat/cross-platform-migration`)
- [x] `J1-2` Mettre en place `pytest` (dossier `tests/unit/`, `tests/functional/`, venv local `.venv` déjà ignoré par `.gitignore`)
- [x] `J1-3` TDD : extraire `domain/transcription.py` (`segments_to_text`, `output_path_for`)
- [x] `J1-4` TDD : extraire `domain/audio_files.py` (`is_supported_audio_extension`, `FILE_DIALOG_PATTERN`)
- [x] `J1-5` TDD : extraire `domain/whisper_models.py` (`is_valid_model_name`, `SUPPORTED_MODELS`, `DEFAULT_MODEL`)
- [x] `J1-6` Réécrire `app_whisper.py` pour consommer `domain/`/`platform_/` sans changer le comportement observable — package renommé `platform_` (et non `platform`) pour ne pas masquer le module standard `platform`, écart au libellé du brief documenté ici pour traçabilité
- [x] `J1-7` Corriger le bug latent : `try/except` dans `platform_/transcriber.py` (`TranscriptionError`), testé (écriture réelle sur disque via `tmp_path`, pas de fichier partiel en cas d'échec), fenêtre de chargement fermée + dialogue d'erreur au lieu d'un blocage silencieux
- [x] `J1-8` Nettoyer `requirements.txt` (`macholib` conditionné à `sys_platform == "darwin"` — `altgraph` est en réalité cross-platform, correction apportée à `AUDIT.md` §3/§4/§7-3 en cours de route), ajout de `requirements-dev.txt`, version Python documentée (`>=3.10,<3.13`)
- [x] `J1-9` Vérification : suite de tests verte localement (16/16) + smoke-test d'import de `app_whisper.py` avec les vraies dépendances installées (venv jetable) confirmant que le câblage domain/platform_/GUI ne casse rien. Pas de lancement interactif complet de la fenêtre Tkinter (pas de session graphique pilotable ici) — la confirmation comportementale complète macOS reste à la charge de l'utilisateur ou de la CI (J2)
- [x] `J2-1` `.github/workflows/ci.yml` : matrice `macos-latest` (Apple Silicon), `windows-latest`, `ubuntu-latest`, exécutant `tests/unit` + `tests/functional`
- [x] `J2-2` Installation de `python3-tk` dans l'étape `ubuntu-latest` de la CI (réf. `AUDIT.md` §4 Linux)
- [x] `J2-3` CI verte confirmée sur les 3 configurations restantes (Linux, Windows, macOS Apple Silicon)
  > `macos-13` (Intel) a été retiré de la matrice après 1h30 sans qu'aucun runner ne lui soit jamais attribué — cause non confirmable depuis l'API publique (pas un problème de quota, pas d'incident GitHub signalé). Décision utilisateur (2026-08-14) : passer à `macos-latest` seul plutôt que de bloquer indéfiniment.
- [x] `J2-4` Couverture Intel restaurée autrement : jobs `test (macOS Intel via Rosetta)` et `e2e (macOS Intel via Rosetta)` — build d'un virtualenv x86_64 sous Rosetta 2 sur `macos-latest` (`arch -x86_64 python3 -m venv ...`), suite complète (unitaires + fonctionnels + E2E réel) exécutée en x86_64 authentique. Vérifié faisable via un job diagnostic (`rosetta-diagnostic`, confirmé : Python `setup-python` est universal2, Rosetta s'installe et fonctionne sur le runner hébergé) avant implémentation, sur la branche `experiment/intel-build-via-rosetta`, mergée dans `feat/cross-platform-migration` après validation verte (run [31787156790](https://github.com/aymnms/transcriber/actions/runs/31787156790)). Détail dans `AUDIT.md` « Annexes — résolution : Intel via Rosetta sur macos-latest ».
- [x] `J3-1` Commande de build PyInstaller Windows ajoutée au README (`assets/logo.ico`)
- [x] `J3-2` Vérifié par CI (job `test (Windows)`, incluant `tests/functional/test_app_entrypoint.py`) : import complet de `app_whisper.py` sans erreur
- [x] `J3-3` README corrigé sur l'état réel de la disponibilité Windows (retrait de la mention d'un `.exe` déjà publié)
- [x] `J4-1` Dépendance système `python3-tk` documentée dans le README
- [x] `J4-2` Commande de build PyInstaller Linux ajoutée au README (`assets/logo.png`)
- [x] `J4-3` Vérifié par CI (job `test (Linux)`, `python3-tk` installé) : import complet sans erreur
- [x] `J5-1` Smoke test E2E (`tests/e2e/test_transcribe_sample_audio.py`, modèle `tiny` réel sur `assets/audios/NewRecording.m4a` via `platform_/transcriber.py`), marqueur pytest `e2e` dédié (hors suite rapide), job CI séparé `e2e` sur la matrice. Validé en local (macOS ARM, dépendances réelles) et en CI (Linux, Windows, macOS Apple Silicon) : transcription correcte du fichier échantillon.
- [x] `J5-2` CI verte simultanément sur macOS Apple Silicon, Windows, Linux (jobs `test` ET `e2e`, run [31784867450](https://github.com/aymnms/transcriber/actions/runs/31784867450), 6/6 jobs verts)
  > Complété par `J2-4` : macOS Intel dispose maintenant de sa propre preuve CI automatisée (via Rosetta sur `macos-latest`), donc la réserve initiale ne s'applique plus — les 4 configurations cibles (macOS Apple Silicon, macOS Intel via Rosetta, Windows, Linux) ont une CI verte.
- [x] `J5-3` README mis à jour (matrice de support par OS, instructions de build Windows/Linux, prérequis Linux, correction de la disponibilité réelle)
- [x] `J5-4` Revue finale de `AUDIT.md`/`PLAN.md` — ce plan est à jour, addendums CI documentés
- [x] `J6-1` `pyproject.toml` : métadonnées projet + config `[tool.semantic_release]` (commits conventionnels déjà utilisés depuis le début de ce travail, `allow_zero_version = true` pour rester en 0.x, `tag_format = "v{version}"` cohérent avec le tag `v0.1.0` existant)
- [x] `J6-2` Validé en local (`--noop`) : calcul correct de la prochaine version (`0.2.0`, cohérent avec les commits `feat:` accumulés depuis `v0.1.0`) et génération du changelog
- [x] `J6-3` `.github/workflows/release.yml` : déclenché après succès de la CI sur `main` (`workflow_run`), job `release` (bump version + changelog + tag + Release GitHub via l'action officielle `python-semantic-release/python-semantic-release`), puis jobs `build-macos` (ARM + Intel via Rosetta), `build-windows`, `build-linux` qui buildent et attachent les binaires PyInstaller à la Release via `gh release upload`
- [x] `J6-4` Validation réelle sur `main`, avec confirmation explicite de l'utilisateur avant déclenchement : [v0.2.0](https://github.com/aymnms/transcriber/releases/tag/v0.2.0) publiée automatiquement — tag créé, `CHANGELOG.md` généré, 4 binaires attachés (`Transcriber-macOS-ARM.zip` 60.6 MB, `Transcriber-macOS-Intel.zip` 83.4 MB, `Transcriber-Windows.zip` 94.5 MB, `transcriber-Linux.tar.gz` 132.8 MB). Pipeline de bout en bout validé, 4/4 jobs verts (run [31790846173](https://github.com/aymnms/transcriber/actions/runs/31790846173)).
- [x] `J7-1` Empaquetage AppImage pour Linux — `packaging/linux/transcriber.desktop`, job `build-linux` de `release.yml` assemble l'AppDir et build via `appimagetool` (`--appimage-extract` pour éviter la dépendance à FUSE, absent des runners GitHub), attaché à chaque release
- [x] `J7-2` Installeur Windows (Inno Setup) — `packaging/windows/installer.iss`, compilé via `iscc` (préinstallé sur `windows-latest`), version injectée dynamiquement (`/DMyAppVersion=...`), attaché à chaque release
- [x] `J8-1` Doublon `MyIcon.icns` à la racine (identique à `assets/MyIcon.icns`, non référencé) — supprimé, instructions de régénération de l'icône corrigées pour écrire directement dans `assets/`
- [x] `J8-2` Branche morte `docker-version` — supprimée du remote
- [x] `J8-3` Ajout de `ruff` (lint + tri des imports + formatage) — codebase reformaté une fois (`ruff format .`), job `lint` ajouté en tête de `ci.yml`
- [x] `J9-1` Corriger le blocage UI Windows : `transcribe_task()` ne mute plus aucun widget Tk depuis le thread d'arrière-plan ; résultat transmis via `queue.Queue` + polling `loader.after()` sur le thread principal. Testé (suite unitaire/fonctionnelle verte, lint propre), confirmé fonctionnel sur Linux réel avec le même code (le tarball se lance et transcrit correctement).
  > Sur Windows, le blocage a persisté après ce correctif — diagnostic plus poussé nécessaire, voir `J9-4`.
- [x] `J9-2` Corriger le binaire Linux lié à une glibc trop récente : `build-linux` (`release.yml`) reconstruit dans un conteneur `ubuntu:22.04` (glibc 2.35) plutôt que directement sur `runs-on: ubuntu-latest` (glibc 2.39+). `actions/setup-python@v5` abandonné au profit du `python3`/`venv` fourni par apt dans le conteneur (Python 3.10, dans la plage déjà documentée `>=3.10,<3.13`) après avoir constaté que `setup-python` échouait silencieusement dans ce conteneur (`gh` CLI installé via apt pour la même raison — absent de l'image de base). Validé par reproduction locale complète via Docker (`--platform linux/amd64`, émulation QEMU sur la machine de dev Apple Silicon) : `pip install` + build PyInstaller réussissent, glibc requise plafonnée à `GLIBC_2.35` sur tous les binaires produits (vérifié via `strings … | grep GLIBC`), pipeline AppImage/`gh` validé à l'identique (seule l'exécution de `appimagetool.AppImage` échoue sous l'émulation QEMU locale — limite de l'environnement de reproduction, sans impact sur le vrai runner x86_64 GitHub, où cette même commande fonctionne déjà aujourd'hui dans le pipeline en production). Job diagnostic temporaire retiré de `ci.yml` une fois la reproduction locale jugée suffisante.

---

## État final (2026-08-14)

**MVP portable atteint intégralement**, sans réserve : l'application se lance et exécute la transcription de bout en bout, vérifié par CI verte sur les **quatre configurations cibles du brief** — macOS Apple Silicon, macOS Intel (via Rosetta 2 sur `macos-latest`), Windows et Linux (`test` + `e2e`, run [31787156790](https://github.com/aymnms/transcriber/actions/runs/31787156790), 8/8 jobs verts). Le runner `macos-13` reste inutilisable côté GitHub, mais s'est révélé non nécessaire : l'exécution x86_64 sous Rosetta sur un runner Apple Silicon est une vérification tout aussi authentique.

Au-delà du MVP, la release automatisée (J6) et le packaging avancé (J7 : AppImage Linux, installeur Windows) ont également été livrés et validés en conditions réelles : trois releases publiées à ce jour, [v0.1.0](https://github.com/aymnms/transcriber/releases/tag/v0.1.0) → [v0.2.0](https://github.com/aymnms/transcriber/releases/tag/v0.2.0) → [v0.3.0](https://github.com/aymnms/transcriber/releases/tag/v0.3.0), cette dernière avec 6 binaires attachés (macOS ARM, macOS Intel, Windows, Linux, AppImage, installeur Windows). Une revue de qualité additionnelle (J8) a nettoyé les derniers résidus identifiés lors de la relecture finale de l'audit.

Aucune régression macOS introduite : le comportement observable de `app_whisper.py` est identique à l'original sur le chemin nominal, avec un bug latent corrigé (blocage silencieux en cas d'échec de transcription) et une dépendance PyPI rompue corrigée (`av`).

Seul point du backlog encore ouvert : le codesign/notarization macOS (`BL-1`), bloqué par l'absence de compte Apple Developer Program payant.

---

## Journal

- 2026-08-14 — Audit complet réalisé (`AUDIT.md`). Aucune question bloquante identifiée : les deux points de décision du brief (accélération matérielle, format de distribution) sont tranchés directement par l'audit.
- 2026-08-14 — Plan de migration rédigé (`PLAN.md`), démarrage du jalon J1.
- 2026-08-14 — J1 terminé : `domain/` (transcription, audio_files, whisper_models) extrait en TDD avec 13 tests unitaires ; `platform_/transcriber.py` ajouté (3 tests fonctionnels) et corrige le blocage silencieux en cas d'échec de transcription ; `app_whisper.py` recâblé sans changement de comportement nominal ; `requirements.txt` nettoyé et complété par `requirements-dev.txt`. Suite complète verte (16/16). Démarrage de J2 (CI multi-OS).
- 2026-08-14 — `J2-1` poussé (`.github/workflows/ci.yml`), premier run CI : échec identique sur Linux/Windows/macOS ARM dès l'installation des dépendances → `av==14.3.0` n'existe plus sur PyPI (dependency rot préexistant, indépendant du portage, cf. `AUDIT.md` annexes). Corrigé en `av==14.2.0`. Re-push : Linux, Windows, macOS Apple Silicon verts. macOS Intel (`macos-13`) reste en `queued` de façon prolongée — capacité de runners Intel limitée côté GitHub Actions actuellement, indépendant de ce dépôt. `> ⚠️ Bloqué (temporaire, infra externe) : en attente que le runner macos-13 soit assigné par GitHub.`
- 2026-08-14 — README corrigé (retrait de la fausse mention `.exe` déjà disponible, ajout du prérequis `python3-tk` Linux, sections de build Windows/Linux). `J5-1` ajouté : test E2E réel (`tests/e2e`), validé en local avec les vraies dépendances (téléchargement + transcription réussie du fichier échantillon), job CI `e2e` ajouté sur la même matrice 4 OS.
- 2026-08-14 — Ajout d'un groupe `concurrency` à la CI pour annuler les runs redondants. État confirmé sur le run le plus à jour : `test` et `e2e` verts sur Linux, Windows, macOS Apple Silicon (6/8 jobs). Les 2 jobs `macos-13` restent `queued` sans assignation de runner malgré plusieurs runs et plus d'une heure d'attente — dépôt public donc pas un problème de quota/facturation. Cause exacte non confirmée (pas d'incident GitHub signalé, pas de dépréciation annoncée trouvée). Question posée à l'utilisateur sur la marche à suivre → réponse initiale : continuer à attendre.
- 2026-08-14 — Après 30 minutes supplémentaires de vérification (1h30 cumulée), toujours aucun mouvement sur `macos-13`. Nouvelle question posée à l'utilisateur → décision : retirer `macos-13` de la matrice CI, garder `macos-latest` (Apple Silicon) comme seul signal macOS. J2 et `J5-2` marqués terminés sur cette base. Écart documenté dans `AUDIT.md` et en introduction de ce plan.
- 2026-08-14 — Push du retrait de `macos-13` : run [31784867450](https://github.com/aymnms/transcriber/actions/runs/31784867450) vert de bout en bout en ~90s (6/6 jobs : `test`+`e2e` sur Linux, Windows, macOS Apple Silicon). README complété (matrice de support par OS). Tous les jalons J1 à J5 sont marqués terminés — MVP portable atteint sur le périmètre CI disponible, réserve macOS Intel documentée et actée avec l'utilisateur.
- 2026-08-14 — Utilisateur : possible de builder Intel sans matériel physique, via Rosetta sur Apple Silicon ? Exploré sur `experiment/intel-build-via-rosetta` (branchée depuis `feat/cross-platform-migration`) : job diagnostic `rosetta-diagnostic` confirmé — Python `setup-python` universal2, Rosetta fonctionnelle sur `macos-latest`. Remplacé par deux jobs réels `test`/`e2e (macOS Intel via Rosetta)` construisant un virtualenv x86_64 sous Rosetta. Run [31787156790](https://github.com/aymnms/transcriber/actions/runs/31787156790) : 8/8 jobs verts, y compris la suite complète et la transcription E2E réelle en x86_64 authentique. Mergé dans `feat/cross-platform-migration` comme convenu. La couverture macOS Intel est désormais complète — plus aucune réserve sur le MVP portable.
- 2026-08-14 — Demande utilisateur (hors brief initial) : mettre en place une release automatisée. Choix validés avec l'utilisateur : `python-semantic-release` (plutôt que le `semantic-release` JS), pipeline complet avec build + attachement des binaires des 4 cibles. Branche `feat/semantic-release` (depuis `feat/cross-platform-migration`) : `pyproject.toml` configuré et validé en `--noop` local (prochaine version calculée : `0.2.0`), `.github/workflows/release.yml` écrit (déclenché après CI verte sur `main` uniquement). La partie « bump + tag + Release GitHub » n'a volontairement pas encore été déclenchée pour de vrai — ça créerait une vraie Release publique, ce qui nécessite une confirmation explicite avant de merger sur `main`.
- 2026-08-14 — Confirmation explicite obtenue de l'utilisateur pour merger jusqu'à `main` et déclencher la première release réelle. Merge `feat/semantic-release` → `feat/cross-platform-migration` → `main`, CI verte (8/8) sur `main`, workflow `Release` déclenché automatiquement : [v0.2.0](https://github.com/aymnms/transcriber/releases/tag/v0.2.0) publiée avec succès, `CHANGELOG.md` généré, 4 binaires attachés (macOS ARM, macOS Intel via Rosetta, Windows, Linux). Pipeline de release automatisée validé de bout en bout en conditions réelles, y compris son comportement de non-déclenchement sur un commit `docs:` (jobs de build passés en `skipped`). Le plan de migration multiplateforme et la release automatisée sont tous deux terminés.
- 2026-08-14 — Remarque utilisateur : un changement purement documentaire (README seul) déclenchait quand même toute la CI (tests + build sur 4 plateformes), alors que `python-semantic-release` sait déjà ne pas releaser sur un commit `docs:`. Ajout d'un filtre `paths-ignore` (`**/*.md`, `LICENSE`) sur les déclencheurs `push`/`pull_request` de `ci.yml` : un push qui ne touche que des fichiers Markdown (ou `LICENSE`) ne déclenche plus du tout la CI, donc pas non plus le workflow `Release` (qui dépend de la complétion de la CI) — économie de CI cohérente avec ce que la logique de versioning faisait déjà.
- 2026-08-14 — Refonte complète du README (branche `docs/readme-revamp`), relue et validée par l'utilisateur, mergée sur `main` sans déclencher de CI (fichier Markdown seul).
- 2026-08-14 — Discussion sur le codesign/notarization macOS (nécessite un compte Apple Developer Program payant, non disponible actuellement — reporté, `BL-1`). Demande utilisateur de traiter les deux autres items du backlog : AppImage Linux et installeur Windows. Branche `feat/appimage-and-windows-installer` : validation empirique via des jobs diagnostic temporaires dans `ci.yml` (`appimage-diagnostic` sur `ubuntu-latest`, `installer-diagnostic` sur `windows-latest`, confirmé qu'Inno Setup 6.7.1 est préinstallé) — les deux verts au premier essai. Intégré dans les jobs `build-linux`/`build-windows` de `release.yml` (attache `transcriber-x86_64.AppImage` et `Transcriber-Setup.exe` à chaque release), jobs diagnostic retirés de `ci.yml`. Jalon `J7` ajouté rétroactivement pour tracer ce travail post-MVP.
- 2026-08-14 — Revue d'architecture objective fournie à l'utilisateur sur demande (sans modification de code) : `domain/`/`platform_/` et la CI/release sont solides, mais `app_whisper.py` (état global, aucun test direct sur la couche GUI, mise à jour de widgets Tkinter depuis un thread en arrière-plan) n'a jamais été refactoré — décision assumée et cohérente avec la contrainte de changements minimaux du brief, pas un oubli. Confirmation utilisateur du merge sur `main` : [v0.3.0](https://github.com/aymnms/transcriber/releases/tag/v0.3.0) publiée avec succès, 6 assets (les 4 précédents + `transcriber-x86_64.AppImage` + `Transcriber-Setup.exe`).
- 2026-08-14 — Relecture de `AUDIT.md` sur demande utilisateur : les 9 recommandations du tableau §7 sont toutes déjà appliquées. 3 points restants identifiés hors tableau (§1, §6) et validés par l'utilisateur : (1) doublon `MyIcon.icns` à la racine (identique à `assets/MyIcon.icns`, non référencé) — supprimé, et instructions de régénération de l'icône corrigées pour écrire directement dans `assets/` ; (2) branche morte `docker-version` — supprimée du remote ; (3) aucun linter/formatter configuré — ajout de `ruff` (lint + tri des imports + formatage), codebase reformaté une fois (`ruff format .`), job `lint` ajouté en tête de `ci.yml`. Regroupé sous jalon `J8` (qualité additionnelle) pour cohérence avec le reste du plan. Seul le codesign/notarization macOS (`BL-1`) reste dans le backlog.
- 2026-08-17 — Réalignement de `AUDIT.md`/`PLAN.md` sur la version actuelle du gabarit du skill (sections numérotées 0-8 pour l'audit, section `## Jalons` explicite et IDs de tâches au format `` `Jn-m` `` pour le plan) — aucun changement de fond, contenu et décisions historiques préservés à l'identique. L'utilisateur prévoit de retester l'application sur l'ensemble des OS cibles ; retour attendu avant toute nouvelle action.
- 2026-08-17 — Retour de test réel utilisateur : (1) Windows — la fenêtre « transcription en cours » reste bloquée indéfiniment (modèle `tiny`, audio d'exemple du dépôt) ; (2) Linux — le binaire tarball et l'AppImage refusent tous deux de démarrer (`GLIBC_2.38' not found`). Jalon `J9` ouvert. Cause Windows identifiée par lecture de code : mutation de widgets Tk depuis le thread d'arrière-plan (risque déjà noté sans preuve lors de la revue d'architecture du 2026-08-14, désormais confirmé). Cause Linux identifiée : `build-linux` compile sur `ubuntu-latest` (Ubuntu 24.04, glibc 2.39), trop récent pour la plupart des systèmes utilisateurs. Détail complet en `AUDIT.md` addenda. `J9-1` corrigé (queue thread-safe + `after()`), suite de tests verte.
- 2026-08-17 — Utilisateur : le tarball Linux fonctionne (`J9-2` confirmé), mais Windows reste bloqué malgré `J9-1`. Ajout de traces `[DEBUG]` temporaires et d'un build Windows console (sans `--windowed`) pour observer le comportement réel plutôt que deviner à nouveau. Sortie console fournie par l'utilisateur : blocage juste après `config.json` (téléchargement du modèle Hugging Face), avant tout octet du fichier de poids — confirme qu'il ne s'agit plus du bug de threading Tk (déjà exclu par Linux) mais d'un blocage réseau pendant le téléchargement du modèle. `J9-4` ouvert, `HF_HUB_DOWNLOAD_TIMEOUT` resserré à 8s (fixé avant l'import de `faster_whisper` dans `app_whisper.py`) pour borner l'attente à ~90s max au lieu d'un blocage potentiellement indéfini si le réseau de l'utilisateur droppe silencieusement les paquets. Limite explicite : ceci ne peut pas garantir une résolution si le blocage est un vrai « black hole » réseau — seul un test sur un autre réseau le confirmera.
- 2026-08-17 — Utilisateur : « Déclenche un build manuel depuis cette branche » pour tester avant fusion. Ajout de `.github/workflows/manual-build.yml` (temporaire, déclenché par push sur cette branche, publie des artefacts CI plutôt qu'une vraie Release — pas de tag). Résultat : job Windows vert (`Transcriber-Windows.zip`, 89.7 MB, prêt à tester), job Linux en échec sur l'étape « Build AppImage » (le tarball, lui, se construit sans erreur — c'est bien la partie qui porte le correctif `J9-2` glibc). Cause non encore identifiée (logs indisponibles sans connexion GitHub). `J9-3` ouvert : c'est bloquant pour la fusion, car `release.yml` reproduirait le même échec en production. Workflow de test ajusté pour uploader le tarball indépendamment de l'AppImage (`continue-on-error` + upload séparé) afin de ne pas priver l'utilisateur du binaire testable pendant que l'AppImage se diagnostique séparément.
- 2026-08-17 — `J9-2` : job diagnostic `linux-glibc-diagnostic` poussé sur `fix/windows-hang-and-linux-glibc` pour valider un build en conteneur `ubuntu:22.04` avant de toucher au pipeline de release réel (le runner `ubuntu-22.04` lui-même entre en dépréciation le 2026-09-17, donc pas retenu comme `runs-on:` direct — vérifié via `actions/runner-images`). Premier run bloqué en boucle sur l'étape d'installation (`apt-get install python3-tk` déclenche une invite interactive `tzdata` sans `DEBIAN_FRONTEND=noninteractive`) — corrigé, nouveau run poussé. Deuxième run : échec net sur l'étape `Build bundle`, logs inaccessibles via l'API publique ni via le navigateur sans authentification (GitHub exige désormais une connexion pour consulter les logs des runs, y compris sur un dépôt public). Reproduction complète en local via Docker (image `ubuntu:22.04`, `--platform linux/amd64` pour égaler l'architecture x86_64 des runners malgré la machine de dev Apple Silicon) : `pip install` et le build PyInstaller réussissent sans erreur avec le `python3` fourni par apt dans le conteneur (3.10.12) — la cause de l'échec CI est donc spécifique à `actions/setup-python@v5` dans ce conteneur, pas aux dépendances du projet. Solution retenue : abandonner `actions/setup-python` pour ce job et utiliser directement `python3`/`venv` du conteneur (Python 3.10, déjà dans la plage supportée du projet), plus `gh` installé via apt (absent de l'image de base, nécessaire pour l'upload de release). Glibc requise confirmée plafonnée à `GLIBC_2.35` sur tous les binaires produits (`strings … | grep GLIBC`). Correctif définitif appliqué à `release.yml` (`build-linux`), job diagnostic retiré de `ci.yml`, YAML validé. `J9` terminé — reste à attendre la validation de l'utilisateur sur ses machines Windows et Linux réelles avant toute fusion sur `main`.
